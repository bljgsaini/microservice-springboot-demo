server:
  port: 8095


spring:
  application:
    name: HYSTRIX-DASHBOARD

management:
  endpoints:
    web:
      exposure:
        include= "*":

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/